# EXPensio - Expense Tracker Application

แอปพลิเคชันจัดการค่าใช้จ่ายแบบ Full-stack พร้อม Dashboard, หมวดหมู่, การกรองตามเวลา, และฟีเจอร์ครบครัน

Link Live Demo : https://expense-tracker-phi-drab-82.vercel.app/

## 📋 สารบัญ

- [ฟีเจอร์หลัก](#-ฟีเจอร์หลัก)
- [เทคโนโลยีที่ใช้](#-เทคโนโลยีที่ใช้)
- [การติดตั้งและรัน](#-การติดตั้งและรัน)
- [การใช้งาน](#-การใช้งาน)
- [API Documentation](#-api-documentation)
- [CI/CD](#-cicd)
- [การ Deploy](#-การ-deploy)
- [Security](#-security)
- [License](#-license)

---

## ✨ ฟีเจอร์หลัก

### 📊 Dashboard
- **Dark Theme UI** - ธีมมืดที่สวยงามและทันสมัย
- **Sidebar Navigation** - เมนูนำทางที่สะดวก พร้อมโปรไฟล์ผู้ใช้
- **Summary Cards** - แสดงสถิติค่าใช้จ่ายแบบ Real-time
  - Total Expenses: ยอดรวมค่าใช้จ่ายทั้งหมด
  - Transactions: จำนวนรายการค่าใช้จ่าย
  - Average per Transaction: ค่าเฉลี่ยต่อรายการ
  - Recent Receipts: ใบเสร็จล่าสุด
- **Charts & Graphs** - กราฟแสดงค่าใช้จ่ายตามหมวดหมู่และรายวัน
  - Team Spending Trend: กราฟแท่งแสดงค่าใช้จ่ายตามหมวดหมู่
  - Day-to-Day Expenses: กราฟแท่งแสดงค่าใช้จ่ายรายวัน
- **Pending Tasks** - แสดงงานที่รอดำเนินการ
  - New Trips Registered
  - Unreported Expenses
  - Expenses with Receipts

### 💰 การจัดการค่าใช้จ่าย
- เพิ่ม/แก้ไข/ลบค่าใช้จ่าย
- จัดหมวดหมู่ค่าใช้จ่าย (Food, Transportation, Shopping, Entertainment, Bills, Health, Other)
- กรองและค้นหาตามช่วงเวลา, หมวดหมู่, และคำค้นหา
- เรียงลำดับตามวันที่หรือจำนวนเงิน
- Export เป็นไฟล์ CSV พร้อมสรุปข้อมูลภาษาไทย

### 📸 ใบเสร็จ (Receipts)
- อัพโหลดรูปภาพใบเสร็จ (รองรับ JPG, PNG, GIF สูงสุด 5MB)
- ดูใบเสร็จที่บันทึกไว้พร้อม preview
- เชื่อมโยงใบเสร็จกับค่าใช้จ่าย
- แสดงภาพ preview ก่อนบันทึก

### ✈️ การเดินทาง (Trips)
- สร้างการเดินทางพร้อมบันทึกค่าใช้จ่ายเริ่มต้น
- แก้ไขและลบการเดินทาง
- เพิ่มค่าใช้จ่ายเข้าไปในการเดินทาง
- ดูสรุปค่าใช้จ่ายของแต่ละการเดินทาง
- แสดงการเดินทางทั้งหมดใน Modal
- จัดการค่าใช้จ่ายของแต่ละการเดินทาง

### 👤 โปรไฟล์ (Profile)
- แก้ไขชื่อ-นามสกุล
- เปลี่ยนรหัสผ่าน
- อัพโหลดรูปโปรไฟล์ (Avatar)
- Preview รูปภาพก่อนบันทึก
- อัพเดทข้อมูลแบบ Real-time

### 🔍 การค้นหาและกรอง
- ค้นหาค่าใช้จ่ายตามคำค้นหา (หมวดหมู่, คำอธิบาย, จำนวนเงิน)
- กรองตามช่วงเวลา (Week, Month, Year, Custom)
- กรองตามหมวดหมู่
- เรียงลำดับตามวันที่หรือจำนวนเงิน (Ascending/Descending)

---

## 🛠️ เทคโนโลยีที่ใช้

### Frontend
- **React 19** - UI Library
- **React Router DOM v7** - Routing
- **Vite** - Build tool และ dev server
- **Recharts** - Data visualization (PieChart, BarChart)
- **Lucide React** - Icon library
- **Axios** - HTTP client
- **date-fns** - Date manipulation

### Backend
- **Node.js** - Runtime environment
- **Express.js v5** - Web framework
- **Prisma ORM** - Database ORM
- **PostgreSQL** - Relational database
- **JWT** (jsonwebtoken) - Authentication
- **bcryptjs** - Password hashing
- **CORS** - Cross-Origin Resource Sharing

### DevOps
- **GitHub Actions** - CI/CD pipeline
- **ESLint** - Code linting

---

## 📦 การติดตั้งและรัน

### Prerequisites
- **Node.js** (v18 หรือสูงกว่า)
- **PostgreSQL Database** (v12 หรือสูงกว่า)
- **npm** หรือ **yarn**

### 1. Clone Repository

```bash
git clone <repository-url>
cd expense-tracker
```

### 2. ติดตั้ง Backend

```bash
cd backend
npm install
```

### 3. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์ `backend/`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/expense_tracker?schema=public"
JWT_SECRET="your-super-secret-jwt-key-here-minimum-32-characters"
PORT=5000
```

**หมายเหตุ**: 
- แทนที่ `DATABASE_URL` ด้วย connection string ของคุณ
- สร้าง `JWT_SECRET` ที่แข็งแกร่ง (แนะนำให้ใช้: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`)

### 4. ตั้งค่า Database

```bash
# Generate Prisma Client
npx prisma generate

# Run database migrations (สำหรับ production)
npx prisma migrate deploy

# หรือ push schema โดยตรง (สำหรับ development/testing)
npx prisma db push
```

### 5. รัน Backend Server

```bash
# Development mode (with nodemon)
npm run dev

# Production mode
npm start
```

Backend จะรันที่ `http://localhost:5000`

### 6. ติดตั้ง Frontend

```bash
cd ../frontend
npm install
```

### 7. ตั้งค่า Frontend Environment

สร้างไฟล์ `.env` ในโฟลเดอร์ `frontend/` (ถ้าต้องการ):

```env
VITE_API_URL=http://localhost:5000/api
```

### 8. รัน Frontend

```bash
# Development mode
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

Frontend จะรันที่ `http://localhost:5173`

---

## 🚀 การใช้งาน

### การสมัครสมาชิกและเข้าสู่ระบบ

1. ไปที่หน้า Register (`/register`)
2. กรอกข้อมูล: ชื่อ-นามสกุล, อีเมล, รหัสผ่าน
3. คลิก "Create Account"
4. หลังจากสมัครสมาชิกสำเร็จ จะเข้าสู่ระบบอัตโนมัติ

### การเพิ่มค่าใช้จ่าย

1. คลิกที่ปุ่ม **"New expense"** ใน Quick Access section หรือปุ่ม **"Add Expense"** ในตาราง
2. กรอกข้อมูล:
   - **Amount**: จำนวนเงิน (บังคับ)
   - **Category**: เลือกหมวดหมู่ (บังคับ)
   - **Date**: เลือกวันที่ (บังคับ)
   - **Description**: คำอธิบาย (ไม่บังคับ)
   - **Trip**: เลือกการเดินทาง (ไม่บังคับ)
3. คลิก **"Add Expense"**

### การแก้ไขค่าใช้จ่าย

1. คลิกที่ปุ่ม **Edit** (ไอคอนดินสอ) ในแถวของรายการที่ต้องการแก้ไข
2. แก้ไขข้อมูลที่ต้องการ
3. คลิก **"Update Expense"**

### การลบค่าใช้จ่าย

1. คลิกที่ปุ่ม **Delete** (ไอคอนถังขยะ) ในแถวของรายการที่ต้องการลบ
2. ยืนยันการลบ

### การอัพโหลดใบเสร็จ

1. คลิกที่ปุ่ม **"Add receipt"** ใน Quick Access section
2. กรอกข้อมูลค่าใช้จ่าย (Amount, Category, Date, Description)
3. คลิก **"Choose File"** และเลือกไฟล์รูปภาพ (JPG, PNG, GIF สูงสุด 5MB)
4. ดู preview รูปภาพ
5. คลิก **"Upload Receipt"** เพื่อบันทึก

### การสร้างการเดินทาง

1. คลิกที่ปุ่ม **"Create trip"** ใน Quick Access section
2. กรอกข้อมูล:
   - **Trip Name**: ชื่อการเดินทาง (บังคับ)
   - **Description**: คำอธิบาย (ไม่บังคับ)
   - **Start Date**: วันที่เริ่มต้น (บังคับ)
   - **End Date**: วันที่สิ้นสุด (ไม่บังคับ)
3. เพิ่มค่าใช้จ่ายเริ่มต้น (ถ้าต้องการ):
   - Amount, Category, Date, Description
4. คลิก **"Create Trip"**

### การจัดการการเดินทาง

1. คลิกที่ปุ่ม **"View Trips"** เพื่อดูการเดินทางทั้งหมด
2. ในแต่ละการเดินทางสามารถ:
   - แก้ไขการเดินทาง (คลิก Edit)
   - ลบการเดินทาง (คลิก Delete)
   - เพิ่มค่าใช้จ่ายเข้าไปในการเดินทาง (คลิก "Add Expense to Trip")

### การค้นหาค่าใช้จ่าย

1. พิมพ์คำค้นหาในช่อง **Search box** ด้านบน
2. รายการค่าใช้จ่ายจะถูกกรองอัตโนมัติตาม:
   - ชื่อหมวดหมู่
   - คำอธิบายค่าใช้จ่าย
   - จำนวนเงิน

### การกรองและเรียงลำดับ

1. **เลือกช่วงเวลา**: คลิก Week/Month/Year หรือเลือก Custom Date Range
2. **เลือกหมวดหมู่**: เลือกจาก dropdown "All Categories"
3. **เรียงลำดับ**: เลือกจาก dropdown Sort (Date หรือ Amount, Ascending หรือ Descending)

### การ Export ข้อมูล

1. คลิกที่ปุ่ม **"Create report"** หรือ **"Export"**
2. ไฟล์ CSV จะถูกดาวน์โหลดอัตโนมัติโดยมีชื่อไฟล์เป็น `expenses_YYYY-MM-DD.csv`
3. ไฟล์จะประกอบด้วย:
   - ข้อมูลค่าใช้จ่ายทั้งหมด (วันที่, หมวดหมู่, คำอธิบาย, จำนวนเงิน, การเดินทาง)
   - สรุปตามหมวดหมู่
   - สรุปยอดรวม (จำนวนรายการทั้งหมด, ยอดรวมทั้งหมด)
   - ภาษาไทย

### การแก้ไขโปรไฟล์

1. คลิกที่ **"Profile"** ใน Sidebar
2. แก้ไขข้อมูล:
   - **Name**: ชื่อ-นามสกุล
   - **Avatar**: อัพโหลดรูปโปรไฟล์ใหม่ (คลิก "Change Avatar")
   - **Password**: เปลี่ยนรหัสผ่าน (กรอกรหัสผ่านใหม่และยืนยัน)
3. คลิก **"Update Profile"**

### 💡 Tips สำหรับการใช้งาน

- **ใช้ Quick Access**: ใช้ปุ่ม Quick Access เพื่อเข้าถึงฟีเจอร์หลักได้เร็วขึ้น
- **ใช้ Search**: ใช้ Search box เมื่อมีรายการเยอะ เพื่อค้นหาอย่างรวดเร็ว
- **Export เป็นประจำ**: Export ข้อมูลเป็นประจำเพื่อเก็บเป็นข้อมูลสำรอง
- **ใช้ Date Range**: ใช้ Date Range buttons เพื่อดูข้อมูลในช่วงเวลาต่างๆ ได้ง่าย
- **ดู Statistics**: ดู Summary Cards เพื่อเข้าใจภาพรวมของค่าใช้จ่าย

---

## 📝 API Documentation

### Base URL
```
http://localhost:5000/api
```

### Authentication
Authentication ใช้ JWT Token ที่ส่งผ่าน header:
```
Authorization: Bearer <token>
```

### Endpoints

#### Authentication
- **POST** `/api/auth/register` - สมัครสมาชิก
  ```json
  {
    "name": "John Doe",
    "email": "john@example.com",
    "password": "password123"
  }
  ```

- **POST** `/api/auth/login` - เข้าสู่ระบบ
  ```json
  {
    "email": "john@example.com",
    "password": "password123"
  }
  ```

- **PUT** `/api/auth/profile` - อัพเดทโปรไฟล์ (Requires Auth)
  ```json
  {
    "name": "John Updated",
    "password": "newpassword123",
    "avatar": "data:image/jpeg;base64,..."
  }
  ```

#### Categories
- **GET** `/api/categories` - ดึงหมวดหมู่ทั้งหมด (Requires Auth)
- **POST** `/api/categories` - สร้างหมวดหมู่ใหม่ (Requires Auth)
  ```json
  {
    "name": "Food",
    "icon": "🍔",
    "color": "#FF6384"
  }
  ```
- **PUT** `/api/categories/:id` - แก้ไขหมวดหมู่ (Requires Auth)
- **DELETE** `/api/categories/:id` - ลบหมวดหมู่ (Requires Auth)

#### Expenses
- **GET** `/api/expenses` - ดึงค่าใช้จ่าย (Requires Auth)
  - Query Parameters:
    - `startDate`: วันที่เริ่มต้น (YYYY-MM-DD)
    - `endDate`: วันที่สิ้นสุด (YYYY-MM-DD)
    - `categoryId`: ID หมวดหมู่
    - `sortBy`: date หรือ amount
    - `sortOrder`: asc หรือ desc
  
- **POST** `/api/expenses` - สร้างค่าใช้จ่ายใหม่ (Requires Auth)
  ```json
  {
    "amount": 1000,
    "description": "Lunch",
    "date": "2024-01-20",
    "categoryId": "category-id",
    "receiptUrl": "data:image/jpeg;base64,...",
    "tripId": "trip-id"
  }
  ```

- **PUT** `/api/expenses/:id` - แก้ไขค่าใช้จ่าย (Requires Auth)
- **DELETE** `/api/expenses/:id` - ลบค่าใช้จ่าย (Requires Auth)

#### Dashboard
- **GET** `/api/dashboard/summary` - ดึงสรุปข้อมูล (Requires Auth)
  - Query Parameters:
    - `startDate`: วันที่เริ่มต้น (YYYY-MM-DD)
    - `endDate`: วันที่สิ้นสุด (YYYY-MM-DD)

#### Trips
- **GET** `/api/trips` - ดึงการเดินทางทั้งหมด (Requires Auth)
- **GET** `/api/trips/:id` - ดึงการเดินทางตาม ID (Requires Auth)
- **POST** `/api/trips` - สร้างการเดินทางใหม่ (Requires Auth)
  ```json
  {
    "name": "Bangkok Trip",
    "description": "Vacation trip",
    "startDate": "2024-01-20",
    "endDate": "2024-01-25",
    "expenses": [
      {
        "amount": 5000,
        "description": "Hotel",
        "date": "2024-01-20",
        "categoryId": "category-id"
      }
    ]
  }
  ```
- **PUT** `/api/trips/:id` - แก้ไขการเดินทาง (Requires Auth)
- **DELETE** `/api/trips/:id` - ลบการเดินทาง (Requires Auth)

### Response Format

**Success Response:**
```json
{
  "data": {...}
}
```

**Error Response:**
```json
{
  "error": "Error message"
}
```

---

## 🔄 CI/CD

โปรเจกต์นี้มี GitHub Actions CI pipeline ที่จะทำงานอัตโนมัติเมื่อ push หรือสร้าง Pull Request

### CI Pipeline จะตรวจสอบ:

- ✅ **Frontend**:
  - ESLint: ตรวจสอบ code quality
  - Build: ตรวจสอบว่า build สำเร็จ
  - Artifacts: บันทึก build artifacts

- ✅ **Backend**:
  - Prisma Schema Validation
  - Prisma Client Generation
  - Database Schema Push
  - Syntax Check
  - Server Start Test

### การดู CI Status

1. ไปที่ GitHub repository
2. คลิกแท็บ **"Actions"**
3. จะเห็น workflow runs พร้อมสถานะ (✅ Success หรือ ❌ Failure)

---

## 🚀 การ Deploy

ดูรายละเอียดการ Deploy ที่ครบถ้วนใน [DEPLOYMENT.md](./DEPLOYMENT.md)

### Quick Deploy Guide

#### Backend Deployment

1. **ตั้งค่า Environment Variables** บน hosting platform:
   ```env
   DATABASE_URL=postgresql://...
   JWT_SECRET=your-secret-key
   PORT=5000
   ```

2. **Deploy Database**:
   ```bash
   npx prisma migrate deploy
   ```

3. **Start Server**:
   ```bash
   npm start
   ```

#### Frontend Deployment

1. **Build**:
   ```bash
   npm run build
   ```

2. **ตั้งค่า VITE_API_URL**:
   ```env
   VITE_API_URL=https://your-backend-url.com/api
   ```

3. **Deploy `dist/` folder** ไปยัง:
   - Vercel
   - Netlify
   - GitHub Pages
   - หรือ static hosting อื่นๆ

### แพลตฟอร์มที่แนะนำ

- **Backend**: Railway, Render, Heroku, DigitalOcean
- **Frontend**: Vercel, Netlify, Cloudflare Pages

---

## 🔒 Security

### Security Features
- ✅ **Password Hashing**: รหัสผ่านถูก hash ด้วย bcryptjs (10 rounds)
- ✅ **JWT Authentication**: ใช้ JWT token สำหรับ authentication (expires in 7 days)
- ✅ **CORS**: Configured สำหรับ cross-origin requests
- ✅ **Environment Variables**: Sensitive data เก็บใน environment variables
- ✅ **SQL Injection Protection**: ใช้ Prisma ORM ซึ่งป้องกัน SQL injection อัตโนมัติ
- ✅ **Input Validation**: มีการตรวจสอบข้อมูลก่อนบันทึกลง database

### Security Best Practices
- ใช้ `JWT_SECRET` ที่แข็งแกร่งใน production (แนะนำ: 32+ characters, random)
- ตั้งค่า CORS ให้อนุญาตเฉพาะ frontend domain ของคุณ
- ใช้ HTTPS ใน production
- ตรวจสอบและลบข้อมูล sensitive จาก logs
- จำกัดขนาดไฟล์ที่อัพโหลด (ปัจจุบัน: 5MB)

---

## 📄 License

ISC

---

## 👨‍💻 Author

Created for internship evaluation

---

## 📞 Support

หากมีปัญหาหรือคำถาม สามารถ:
- เปิด Issue ใน GitHub repository
- ตรวจสอบ [DEPLOYMENT.md](./DEPLOYMENT.md) สำหรับ troubleshooting

---

**พร้อมใช้งานแล้ว!** 🎉
